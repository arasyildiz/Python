asal_sayilar = []

for sayi in range(2, 1001):
    asal = True
    for i in range(2, int(sayi**0.5) + 1):
        if sayi % i == 0:
            asal = False
            break
    if asal:
        asal_sayilar.append(sayi)

print("0 ile 1000 arasındaki asal sayılar:", asal_sayilar)
